name: Request Copilot Review

on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  copilot-review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      # The previous steps attempted to request a Copilot review, but this is not supported.
      # To enable automated code review, consider using GitHub's CodeQL analysis or a third-party tool.
      # Example: https://docs.github.com/en/code-security/code-scanning/automatically-scanning-code-for-vulnerabilities-and-errors
